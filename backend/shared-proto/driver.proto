syntax = "proto3";

package driver;

option go_package = "./proto";

// Empty service for now
service Driver {
    // Placeholder method
    rpc Ping(PingRequest) returns (PingResponse) {}
}

message PingRequest {
    // Empty
}

message PingResponse {
    // Empty
}