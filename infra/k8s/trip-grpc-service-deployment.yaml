apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-grpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trip-grpc
  template:
    metadata:
      labels:
        app: trip-grpc
    spec:
      containers:
        - name: trip-grpc
          image: uit-go/trip-grpc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50052
