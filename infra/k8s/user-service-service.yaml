apiVersion: v1
kind: Service
metadata:
  name: user-Service
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP # Note: In future, when we deploy this project to AWS, if we need multi-cluster design (for high availability and disastor recovery), switch this.
  